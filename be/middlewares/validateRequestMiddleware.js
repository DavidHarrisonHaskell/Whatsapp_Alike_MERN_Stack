/*
    - login: 
    - check username, email, and password are there and formatted properly
    - check if username is unique in the database
    - check if email is unique in the database
    
    - groups: 
    - validate group actions like groupName, userId existws and only valid data updates the group
    - validate groupID, if a user is already in the group, 

    - messaging: 
    - validate senderId, receiverId, message exist and only valid data updates the message

    - media:
    - limit of size for media files, types of files

    - user:
    -  block/unblock user
    - sending message check if user was blocked before sending

    - chat history:
    - verify senderId and receiverId exist and only valid data updates the chat history
    - pagination parameters: limit, offset are only positive integers
    

*/